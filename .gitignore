.DS_Store
.env.local
andrew-resume-app/

# ScrollProxy runs: commit summaries only, keep heavy archive data local
projects/scrollproxy/runs/**/raw.json
projects/scrollproxy/runs/**/screenshots/

# ScrollProxy morning routine: launchd runtime logs are local-only
logs/scrollproxy-morning/

# Local Claude Code state (settings.local.json, launch configs)
.claude/

# Obsidian per-machine workspace layout (other .obsidian config stays tracked)
.obsidian/workspace.json

# Code projects under projects/ are their own git repos — never track them here
projects/gtm-coach/
